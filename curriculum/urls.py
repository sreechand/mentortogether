#
# copyright (c) 2010 Mentortogether
#
from django.conf.urls.defaults import patterns, url
"""
from mentortogether.curriculum import views

urlpatterns = patterns('',
    url(r'^$', 
        views.main,            name="index"),

    url(r'^add/$', 
        views.add_curriculum,  name="add-curriculum"),

    url(r'^(?P<cid>\d+)/$', 
        views.view_curriculum, name="view-curriculum"),

    url(r'^(?P<cid>\d+)/edit/$', 
        views.edit_curriculum, name="edit-curriculum"),

    url(r'^(?P<cid>\d+)/add/$',        
        views.add_section,     name="add-section"),

    url(r'^(?P<cid>\d+)/(?P<sid>\d+)/$', 
        views.view_section,    name="view-section"),

    url(r'^(?P<cid>\d+)/(?P<sid>\d+)/edit/$', 
        views.edit_section,    name="edit-section"),

    url(r'^(?P<cid>\d+)/(?P<sid>\d+)/add/$', 
        views.add_prompt,      name="add-prompt"),

    url(r'^(?P<cid>\d+)/(?P<sid>\d+)/(?P<pid>\d+)/$', 
        views.edit_prompt,     name="edit-prompt"),
)
    """
